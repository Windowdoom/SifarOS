name: Boot CI

on:
  push:
    branches:
      - main
      - sifar/hardening-v1
      - sifar/2.0-dev
      - sifar/2.0-adaptive-staging
      - sifar/2.0-ui
  pull_request:

jobs:
  build-and-boot-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install toolchain and QEMU
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential gcc-multilib nasm qemu-system-x86 python3

      - name: Strict build
        run: make clean && make

      - name: Run serial boot test suite
        run: make test

      - name: Run GUI regression suite
        run: make test-gui

      - name: Run corrupted SifarFS rejection tests
        run: bash tools/test-sfs-corrupt.sh

      - name: Rebuild clean image for high-memory regression
        run: make clean && make

      - name: Run boot suite with 2 GiB guest RAM
        env:
          MEMORY: 2048
          LOG: build/test-output-2g.log
          LOG2: build/test-reboot-2g.log
        run: make test
